HashSet<int> myHashSet = new HashSet<int> { 1, 2, 3, 4, 5 };

// Add an element
myHashSet.Add(6);

// Remove an element
myHashSet.Remove(3);

// Check existence of an element
bool exists = myHashSet.Contains(4);
